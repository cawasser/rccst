(ns bh.rccst.data-source.counter
  (:require [bh.rccst.websocket :as socket]))


(comment
  (def base-msg {:what-is-this "An async broadcast pushed from server"
                 :how-often    "Every 1 second"
                 :set            i})

  ())